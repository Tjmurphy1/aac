import React, { Component } from 'react';

class Compression extends Component {
    render() {
        return (
            <div className="contain">
                <h4 className="title">Compression</h4>
                <p>
                AAC uses the compression lossless They typical file size for AAC before compression is around 13mb. After compression it’s around 6.7mb. It gets compressed around 50-95%. 
                </p>
            </div>
        );
    }
}

export default Compression;
