import React, { Component } from 'react';

class Similar extends Component {
    render() {
        return (
            <div className="contain">
                <h4 className="title">Similar Formats and the Advantages / Disadvantages</h4>
                <p>
                    Some advantages AAC has over other file formats are that more devices use AAC instead of different file formats, they also have smaller encoded file sizes. AAC also had better sounding audio than other formats when its compressed. AAC produces smaller sized audio files and cuts the bandwidth rates. Some disadvantages AAC have is that it takes longer to encode, higher cpu usage, and not as much of a better sound. AAC is great at making a file size lower and audio quality sound better.  FLAC is known to sound a lot better than AAC because it's lossy. It also supports hi-res
                </p>
            </div>
        );
    }
}

export default Similar;
